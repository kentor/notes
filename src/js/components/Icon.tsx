/* AUTOGENERATED. DO NOT MODIFY. */
/* eslint-disable */
import React from 'react';
import Clipboard from 'App/components/icons/Clipboard.tsx';
import Logout from 'App/components/icons/Logout.tsx';
import Pencil from 'App/components/icons/Pencil.tsx';
import StarOutline from 'App/components/icons/StarOutline.tsx';
import Star from 'App/components/icons/Star.tsx';
import Trashcan from 'App/components/icons/Trashcan.tsx';

const icons = {
  'clipboard': Clipboard,
  'logout': Logout,
  'pencil': Pencil,
  'star-outline': StarOutline,
  'star': Star,
  'trashcan': Trashcan,
};

type Props = {
  icon: keyof typeof icons;
  size?: number;
};

function Icon(props: Props) {
  const IconComponent = icons[props.icon];
  const size = props.size || 13;
  const style = {
    fill: 'currentcolor',
    height: size,
    width: size,
  };
  return (
    <IconComponent preserveAspectRatio="xMidYMid meet" style={style} />
  );
}

export default Icon;
  